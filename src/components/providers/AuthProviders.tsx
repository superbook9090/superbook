'use client';

/** Auth layout shell — settings and maintenance are handled at root layout. */
export default function AuthProviders({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
